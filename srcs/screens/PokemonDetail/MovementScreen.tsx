import React, { FC } from 'react'

import { MovementProp } from '../../types/PokemonProps'

type MovementScreenProps = {
    movement: MovementProp
}

const MovementScreen: FC<MovementScreenProps> = ({ movement }) => {
    return <>

    </>
}

export default MovementScreen
